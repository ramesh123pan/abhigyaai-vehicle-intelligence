(function(){
  const field=(label,name,type='text',extra='')=>`<label>${label}<input name="${name}" type="${type}" ${extra}></label>`;
  function add(){const form=document.querySelector('#settingsForm');if(!form||form.dataset.enhanced)return;form.dataset.enhanced='1';
    const branding=document.createElement('div');branding.className='settings-section';branding.innerHTML='<div><h3>Branding assets</h3><p>Upload a logo from your computer or paste a hosted image URL.</p></div><div class="settings-grid">'+field('Application logo URL','logoUrl')+field('Upload application logo','logoUpload','file','accept="image/png,image/jpeg,image/webp,image/gif"')+field('Login logo URL','loginLogoUrl')+field('Upload login logo','loginLogoUpload','file','accept="image/png,image/jpeg,image/webp,image/gif"')+'</div>';form.insertBefore(branding,form.querySelector('.settings-actions'));
    const login=document.createElement('div');login.className='settings-section';login.innerHTML='<div><h3>Login page</h3><p>Customize the public sign-in screen without changing the shared authentication flow.</p></div><div class="settings-grid">'+field('Login title','loginTitle')+field('Login subtitle','loginSubtitle')+field('Background color','loginBackgroundColor','color')+field('Button color','loginButtonColor','color')+'</div>';form.insertBefore(login,form.querySelector('.settings-actions'));
    const smtp=document.createElement('div');smtp.className='settings-section';smtp.innerHTML='<div><h3>SMTP mail delivery</h3><p>These values are stored for server-side mail features. The SMTP password is never returned to the browser.</p></div><div class="settings-grid">'+field('SMTP host','smtpHost')+field('SMTP port','smtpPort','number','min="1" max="65535"')+field('SMTP username','smtpUser')+field('SMTP password','smtpPassword','password','autocomplete="new-password"')+field('From email','smtpFromEmail','email')+'<label>Security<select name="smtpSecure"><option value="tls">TLS</option><option value="ssl">SSL</option><option value="none">None</option></select></label></div>';form.insertBefore(smtp,form.querySelector('.settings-actions'));
    const read=(input,target)=>input.addEventListener('change',()=>{const file=input.files?.[0];if(!file)return;if(file.size>1500000){input.value='';return}const reader=new FileReader();reader.onload=()=>{const url=form.querySelector(`[name="${target}"]`);if(url)url.value=reader.result};reader.readAsDataURL(file)});
    read(form.elements.logoUpload,'logoUrl');read(form.elements.loginLogoUpload,'loginLogoUrl');
    fetch('/api/settings',{cache:'no-store'}).then(r=>r.ok?r.json():{}).then(s=>{for(const name of ['logoUrl','loginLogoUrl','loginTitle','loginSubtitle','loginBackgroundColor','loginButtonColor','smtpHost','smtpPort','smtpUser','smtpFromEmail','smtpSecure']){const el=form.elements[name];if(el&&s[name]!==undefined)el.value=s[name]||''}}).catch(()=>{});
  }
  const watch=()=>setTimeout(add,300);window.addEventListener('hashchange',watch);window.addEventListener('popstate',watch);window.addEventListener('load',watch);window.setTimeout(add,500);new MutationObserver(add).observe(document.body,{childList:true,subtree:true});
  async function apply(){try{const r=await fetch('/api/settings',{cache:'no-store'});if(!r.ok)return;const s=await r.json();if(s.businessName){document.querySelectorAll('.sidebar-brand span:last-child,.site-footer strong').forEach(x=>x.textContent=s.businessName);document.title=s.businessName}if(s.logoUrl){document.querySelectorAll('.brand-mark').forEach(x=>{x.innerHTML=`<img src="${s.logoUrl}" alt="" style="width:100%;height:100%;object-fit:contain;border-radius:inherit">`})}}catch{}}
  window.setTimeout(apply,700);window.addEventListener('hashchange',()=>window.setTimeout(apply,300));
})();
