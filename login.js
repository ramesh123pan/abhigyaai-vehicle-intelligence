const requestedReturn=new URLSearchParams(location.search).get('returnTo')||'/dashboard';
const safeReturn=/^\/(?:dashboard|search|records|pucc|insurance|fitness|activity|api-docs|api-keys|admins|audit|settings|plans|usage|plan\/\d+)\/?(?:\?[^#]*)?$/.test(requestedReturn)?requestedReturn:'/dashboard';
fetch('/api/public-settings',{cache:'no-store'}).then(r=>r.json()).then(s=>{const root=document.documentElement,form=document.getElementById('signIn'),title=s.businessName||'Vehicle Desk';document.title=`${title} | Sign In`;form.querySelector('h1').textContent=title;if(s.loginSubtitle)form.querySelector('p').textContent=s.loginSubtitle;if(s.logoUrl||s.loginLogoUrl){const img=document.createElement('img');img.src=s.logoUrl||s.loginLogoUrl;img.alt='';img.className='login-logo';form.prepend(img)}if(s.loginBackgroundColor)root.style.setProperty('--login-bg',s.loginBackgroundColor);if(s.loginBackgroundGradientStart&&s.loginBackgroundGradientEnd)root.style.setProperty('--login-bg',`linear-gradient(135deg,${s.loginBackgroundGradientStart},${s.loginBackgroundGradientEnd})`);if(s.loginButtonGradientStart&&s.loginButtonGradientEnd)root.style.setProperty('--login-button',`linear-gradient(135deg,${s.loginButtonGradientStart},${s.loginButtonGradientEnd})`);if(s.loginBackgroundImageUrl)document.body.style.backgroundImage=`url("${s.loginBackgroundImageUrl}")`}).catch(()=>{});
fetch('/api/auth/me',{cache:'no-store'}).then(response=>response.json()).then(data=>{if(data.authenticated)location.replace(safeReturn)}).catch(()=>{});
document.getElementById('signIn').addEventListener('submit',async event=>{
  event.preventDefault();const button=event.target.querySelector('button'),error=document.getElementById('error');button.disabled=true;error.textContent='';
  try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(event.target)))}),data=await response.json();if(!response.ok)throw Error(data.message||'Sign in failed');location.replace(safeReturn)}catch(e){error.textContent=e.message}finally{button.disabled=false}
});
